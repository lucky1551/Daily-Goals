import './Header.css';

// we can use rfc or rfce or rafc to create function easily (ES7 react snippet downloaded)
function Header1(){
    return(
    <nav>What are your goals !!</nav>
    );
}






// by using default we can import file by any name and also we can export specific part or whole file.
// export default Header 
// export default Main

// we can export both (default and specific) from a single file but the rule is you can only export one default
export default Header1;